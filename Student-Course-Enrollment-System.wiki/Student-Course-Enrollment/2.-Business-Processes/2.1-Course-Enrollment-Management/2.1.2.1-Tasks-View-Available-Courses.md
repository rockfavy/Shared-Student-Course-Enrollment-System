# View Available Courses – Task Catalogue
---

## Task 1: Create API CRUD for Course entity (in-memory DB)

**Description:**  
- **Front-End:** N/A  
- **Back-End:** Create `CourseEndpoints.cs` with `MapCourseEndpoints()` extension method. Implement `GET /courses` and `GET /courses/{id}` endpoints. Create `CourseService` for business logic.  
- **Database:** Create `Course` entity with Id, Name, Description, Capacity, CreatedAt. Configure in `EnrollmentContext`.  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CourseEndpoints {
        +MapCourseEndpoints(WebApplication app)
        +GetCoursesEndpoint(CourseService) Task~IResult~
        +GetCourseEndpoint(Guid id, CourseService) Task~IResult~
    }
    
    class CourseService {
        +GetCoursesAsync() Task~List~Course~~
        +GetCourseAsync(Guid id) Task~Course~
    }
    
    CourseEndpoints --> CourseService
    CourseService --> EnrollmentContext
:::

### Entity Diagram

::: mermaid
erDiagram
    Course {
        Guid Id PK
        string Name
        string Description
        int Capacity
        DateTime CreatedAt
    }
:::

---

## Task 2: Seed sample course data

**Description:**  
- **Front-End:** N/A  
- **Back-End:** Create `DataSeeder` service. Implement `SeedCoursesAsync()` method that adds sample courses to in-memory database. Call seeder in `Program.cs` on application startup.  
- **Database:** Insert sample course records into `Courses` DbSet.  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class DataSeeder {
        +SeedCoursesAsync(EnrollmentContext) Task
        +CreateSampleCourses() List~Course~
    }
    
    DataSeeder --> EnrollmentContext
:::

---

## Task 3: Create Blazor page to list available courses

**Description:**  
- **Front-End:** Create `CoursesPage.razor` component. Implement `OnInitializedAsync()` to load courses. Display courses in a list or grid layout. Add loading and error states.  
- **Back-End:** N/A  
- **Database:** N/A  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CoursesPage {
        +List~Course~ Courses
        +bool IsLoading
        +string ErrorMessage
        +OnInitializedAsync() Task
        +EnrollAsync(Guid courseId) Task
    }
    
    CoursesPage --> CoursesClient
:::

---

## Task 4: Call API via HTTP to retrieve course list

**Description:**  
- **Front-End:** Create `CoursesClient` service. Implement `GetCoursesAsync()` method that calls `GET /courses` endpoint. Handle HTTP errors and deserialize response.  
- **Back-End:** N/A  
- **Database:** N/A  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CoursesClient {
        +HttpClient httpClient
        +GetCoursesAsync() Task~List~Course~~
        +GetCourseAsync(Guid id) Task~Course~
    }
    
    CoursesClient --> HttpClient
:::

---

## Task 5: Display course details (name, description)

**Description:**  
- **Front-End:** Create `CourseCard.razor` component to display individual course information. Show course name, description, and capacity. Add styling for visual appeal.  
- **Back-End:** N/A  
- **Database:** N/A  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CourseCard {
        +Course Course
        +Parameter~Course~ Course
        +Render()
    }
    
    CourseCard --> Course
:::

---

## Task 6: Add UI elements to initiate enrollment

**Description:**  
- **Front-End:** Add "Enroll" button to `CourseCard` or `CoursesPage`. Implement `EnrollAsync()` method that calls enrollment endpoint. Show confirmation message after enrollment.  
- **Back-End:** N/A  
- **Database:** N/A  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CourseCard {
        +Course Course
        +EnrollAsync() Task
        +bool IsEnrolling
    }
    
    class EnrollmentsClient {
        +HttpClient httpClient
        +EnrollAsync(Guid courseId) Task~EnrollmentResult~
    }
    
    CourseCard --> EnrollmentsClient
:::

---

