# Maintain CRUD Operations for Courses – Task Catalogue
---

## Task 1: Create Course model

**Description:**  
- **Front-End:** N/A  
- **Back-End:** Create `Course.cs` entity class with properties: Id (Guid), Name (string), Description (string), Capacity (int), CreatedAt (DateTime). Add navigation property `Enrollments`.  
- **Database:** Define `Course` entity structure.  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class Course {
        +Guid Id
        +string Name
        +string Description
        +int Capacity
        +DateTime CreatedAt
        +ICollection~Enrollment~ Enrollments
    }
:::

### Entity Diagram

::: mermaid
erDiagram
    Course {
        Guid Id PK
        string Name
        string Description
        int Capacity
        DateTime CreatedAt
    }
:::

---

## Task 2: Implement in-memory repository

**Description:**  
- **Front-End:** N/A  
- **Back-End:** Configure `Course` in `EnrollmentContext` as `DbSet<Course>`. Use EF Core in-memory provider. Configure entity relationships and constraints in `OnModelCreating()`.  
- **Database:** Add `Courses` DbSet to context. Configure entity mapping.  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class EnrollmentContext {
        +DbSet~Course~ Courses
        +OnModelCreating(ModelBuilder)
    }
    
    EnrollmentContext --> Course
:::

---

## Task 3: Develop CRUD API endpoints

**Description:**  
- **Front-End:** N/A  
- **Back-End:** Create `CourseEndpoints.cs` with `MapCourseEndpoints()` extension method. Implement GET (all), GET (by id), POST (create), PUT (update), DELETE endpoints. Add request/response DTOs.  
- **Database:** N/A  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CourseEndpoints {
        +MapCourseEndpoints(WebApplication app)
        +GetCoursesEndpoint(CourseService) Task~IResult~
        +GetCourseEndpoint(Guid id, CourseService) Task~IResult~
        +CreateCourseEndpoint(CreateCourseRequest, CourseService) Task~IResult~
        +UpdateCourseEndpoint(Guid id, UpdateCourseRequest, CourseService) Task~IResult~
        +DeleteCourseEndpoint(Guid id, CourseService) Task~IResult~
    }
    
    class CourseService {
        +GetCoursesAsync() Task~List~Course~~
        +GetCourseAsync(Guid id) Task~Course~
        +CreateCourseAsync(CreateCourseRequest) Task~Course~
        +UpdateCourseAsync(Guid id, UpdateCourseRequest) Task~Course~
        +DeleteCourseAsync(Guid id) Task~bool~
    }
    
    CourseEndpoints --> CourseService
    CourseService --> EnrollmentContext
:::

---

## Task 4: Enable GET (all + specific), POST, PUT, DELETE

**Description:**  
- **Front-End:** N/A  
- **Back-End:** Implement all HTTP methods: GET `/courses`, GET `/courses/{id}`, POST `/courses`, PUT `/courses/{id}`, DELETE `/courses/{id}`. Add validation and error handling.  
- **Database:** N/A  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CourseEndpoints {
        +GetCoursesEndpoint() Task~IResult~
        +GetCourseEndpoint(Guid id) Task~IResult~
        +CreateCourseEndpoint(CreateCourseRequest) Task~IResult~
        +UpdateCourseEndpoint(Guid id, UpdateCourseRequest) Task~IResult~
        +DeleteCourseEndpoint(Guid id) Task~IResult~
    }
:::

---

## Task 5: Test CRUD logic manually or with basic unit tests

**Description:**  
- **Front-End:** N/A  
- **Back-End:** Create unit tests for `CourseService` methods. Test each CRUD operation. Verify database state after operations.  
- **Database:** N/A  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CourseServiceTests {
        +TestGetCoursesAsync()
        +TestGetCourseAsync()
        +TestCreateCourseAsync()
        +TestUpdateCourseAsync()
        +TestDeleteCourseAsync()
    }
    
    CourseServiceTests --> CourseService
:::

---

## Task 6: Integrate CRUD endpoints with the Blazor UI where required

**Description:**  
- **Front-End:** Update `CoursesClient` to include POST, PUT, DELETE methods. Add UI forms for creating/editing courses if needed. Connect UI actions to API endpoints.  
- **Back-End:** N/A  
- **Database:** N/A  
- **Infrastructure:** N/A

### Class Diagram

::: mermaid
classDiagram
    class CoursesClient {
        +HttpClient httpClient
        +GetCoursesAsync() Task~List~Course~~
        +GetCourseAsync(Guid id) Task~Course~
        +CreateCourseAsync(CreateCourseRequest) Task~Course~
        +UpdateCourseAsync(Guid id, UpdateCourseRequest) Task~Course~
        +DeleteCourseAsync(Guid id) Task~bool~
    }
    
    class CreateCoursePage {
        +CreateCourseRequest Model
        +OnSubmitAsync() Task
    }
    
    CoursesClient --> CreateCoursePage
:::

---




